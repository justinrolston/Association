<h1>Listing contacts</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Phone</th>
    <th>Email</th>
    <th>Business</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% for contact in @contacts %>
  <tr>
    <td><%= contact.name %></td>
    <td><%= contact.phone %></td>
    <td><%= contact.email %></td>
    <td><%= contact.business %></td>
    <td><%= link_to 'Show', business_contact_path(@business, contact) %></td>
    <td><%= link_to 'Edit', edit_business_contact_path(@business, contact) %></td>
    <td><%= link_to 'Destroy', business_contact_path, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Contact', new_business_contact_path(@business) %>
